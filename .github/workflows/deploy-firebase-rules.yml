name: Deploy Firebase Database Rules

on:
  push:
    branches:
      - main
    paths:
      - firebase-database.rules.json
      - firebase.json
      - .github/workflows/deploy-firebase-rules.yml
  workflow_dispatch:

jobs:
  deploy-rules:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Skip when Firebase secret is missing
        if: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON == '' }}
        run: |
          echo "FIREBASE_SERVICE_ACCOUNT_JSON is not configured."
          echo "Add the secret in GitHub Settings > Secrets and variables > Actions."
          echo "Workflow finished without deployment."

      - name: Authenticate to Google Cloud
        if: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON != '' }}
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}

      - name: Setup Node.js
        if: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON != '' }}
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Firebase CLI
        if: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON != '' }}
        run: npm install -g firebase-tools

      - name: Deploy Realtime Database Rules
        if: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON != '' }}
        run: firebase deploy --only database --project growthlock-6a9e3 --non-interactive